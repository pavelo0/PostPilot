generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

// Модели добавим по чеклисту: User (P1), Post (P2), Channel (P3)
