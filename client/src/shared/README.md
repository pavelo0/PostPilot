# shared layer

Reusable infrastructure: UI kit, lib helpers, config, API base client.

**Migration target:** `components/ui/` → `shared/ui/`, `lib/` → `shared/lib/`.

Import rule: may not import from upper layers.
