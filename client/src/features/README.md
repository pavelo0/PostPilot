# features layer

User actions and business interactions.

**Examples:** `features/create-post/`, `features/publish-post/`, `features/connect-channel/`, `features/auth-login/`

Import rule: `entities`, `shared` only.
