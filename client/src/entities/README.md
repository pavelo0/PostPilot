# entities layer

Domain models, types, and API slices (RTK Query).

**Examples:** `entities/post/`, `entities/channel/`, `entities/user/`

**Migration target:** move from `store/api/`, `utils/*/schema.ts`, domain fetch wrappers.

Import rule: `shared` only.
